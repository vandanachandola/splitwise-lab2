const config = {};

config.server = {};
config.server.host = 'http://18.223.2.203';
config.server.port = '5000';
config.server.url = `${config.server.host}:${config.server.port}`;

module.exports = config;
